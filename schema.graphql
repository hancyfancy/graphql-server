type Query  {
   test: String,
   students: [Student!]!
}

type Student {
    id: ID!,
    email: String!,
    firstName: String!,
    lastName: String!,
    password: String!,
    collegeId: String!
}